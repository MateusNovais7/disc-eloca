# Banco de dados
DATABASE_URL="postgresql://disc_eloca:CHANGE_ME@postgres:5432/disc_eloca?schema=public"

# Sessão administrativa (gerar com: openssl rand -base64 32)
ADMIN_SESSION_SECRET="CHANGE_ME"

# Credenciais do admin inicial (usadas apenas no seed)
ADMIN_SEED_EMAIL="admin@eloca.com.br"
ADMIN_SEED_PASSWORD="CHANGE_ME_STRONG_PASSWORD"

# URL pública da aplicação
NEXT_PUBLIC_APP_URL="https://disc.eloca.com.br"
